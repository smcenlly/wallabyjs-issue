import ButtonSelector from './ButtonSelector.vue';

const install = Vue => {
  Vue.component('buttonSelector', ButtonSelector);
};

export default {
  install
};

export { ButtonSelector };
